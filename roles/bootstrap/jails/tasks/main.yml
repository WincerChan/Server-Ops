- name: Disable Syslogd
  lineinfile:
    dest: "{{ jail_dir }}/flavors/default/etc/rc.conf"
    regexp: ^syslogd_enable="YES"
    line: syslogd_enable="NO"

- name: Disable Cron
  lineinfile:
    dest: "{{ jail_dir }}/flavors/default/etc/rc.conf"
    regexp: ^cron_enable="YES"
    line: cron_enable="NO"

- name: Create Jail for Jails Group
  vars:
    jail_ip: hostvars['item']['jail_ip']
    jail_name: "{{item}}"
  include: new.yml
  loop: "{{ groups['jails'] }}"